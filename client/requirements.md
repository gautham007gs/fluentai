## Packages
framer-motion | Complex animations for messages and page transitions
date-fns | Formatting timestamps in chat
react-hook-form | Form state management for creating chats
@hookform/resolvers | Zod resolver for react-hook-form
zod | Schema validation

## Notes
- Using Google Fonts: 'Outfit' for headings, 'Inter' for body text
- Auth logic relies on existing use-auth.ts hook
- Chat API is request/response (not streaming) based on the provided schema manifest
- Tailwind config needs to support the custom font families defined in index.css
